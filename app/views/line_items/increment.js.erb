
//modify the line-item details as well as the cart total.to reflect the addition/subtraction
$('.cart-item[data-id="<%= @line_item.id %>"]').replaceWith("<%= escape_javascript render(@line_item) %>");

$('#cart-total .pull-right').html("<%= number_to_currency(@line_item.cart.total_amount, unit: "\u20A6")%>");